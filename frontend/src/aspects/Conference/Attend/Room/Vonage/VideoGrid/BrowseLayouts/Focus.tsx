import React from "react";
import type { Viewport } from "../../Components/LayoutTypes";

export function Focus(_: { viewports: Viewport[]; focusViewports: Viewport[] }): JSX.Element {
    return <></>;
}
