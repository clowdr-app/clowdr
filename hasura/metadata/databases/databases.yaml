- name: default
  kind: postgres
  configuration:
    connection_info:
      database_url:
        from_env: HASURA_GRAPHQL_DATABASE_URL
      isolation_level: read-committed
      pool_settings:
        connection_lifetime: 600
        idle_timeout: 180
        max_connections: 225
        retries: 3
      use_prepared_statements: true
    read_replicas:
    - database_url:
        from_env: POSTGRES_REPLICA_1_DATABASE_URL
      pool_settings:
        idle_timeout: 180
        max_connections: 225
        retries: 3
  tables: "!include default/tables/tables.yaml"
  functions: "!include default/functions/functions.yaml"
