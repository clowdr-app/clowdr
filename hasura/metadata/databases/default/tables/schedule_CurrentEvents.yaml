table:
  schema: schedule
  name: CurrentEvents
object_relationships:
  - name: room
    using:
      manual_configuration:
        remote_table:
          schema: room
          name: Room
        insertion_order: null
        column_mapping:
          roomId: id
array_relationships:
  - name: eventPeople
    using:
      manual_configuration:
        remote_table:
          schema: schedule
          name: EventProgramPerson
        insertion_order: null
        column_mapping:
          id: eventId
